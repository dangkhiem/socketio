import express from 'express';
// import * as bookController from '../controllers/bookController.js'; 

const bookRouter = express.Router();

// bookRouter.get('/login', bookController.login);

export default bookRouter;